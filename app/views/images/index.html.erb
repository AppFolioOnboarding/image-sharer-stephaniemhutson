<p>Number of images: <%= @images ? @images.length : 0 %></p>

<% if @images %>
  <ul>
    <% @images.each do |image| %>
      <li><%= image.image_url %>
    <% end %>
  </ul>
<% end %>

<p>Link to image submission form <%= link_to "here", new_image_path %>
